{% extends __DIR__ . '/../template/app.twig' %}

{% block content %}
<div class="container signup">
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <div class="card">
                <div class="card-header">Sign up!</div>
                <div class="card-body">

                    <?php //var_dump($_SESSION['errors']);?>

                    <form action="/auth/signup" method="post">
                        <div class="form-group has-error">
                            <label for="email">Email</label>
                            <input type="email" name="email" id="email" placeholder="you@domain.com" class="form-control" >
                            <?php
                                if (isset($_SESSION['errors']['email'])):
                                $errorEmail = $_SESSION['errors']['email'][0];
                            ?>
                                <small><span class="form-text text-muted"><?php echo $errorEmail; ?></span></small>
                            <?php endif; ?>
                        </div>
                        <div class="form-group">
                            <label for="name">Name</label>
                            <input type="text" name="name" id="name" placeholder="Jhon Doe" class="form-control" >
                            <?php
                            if (isset($_SESSION['errors']['name'])):
                                $errorName = $_SESSION['errors']['name'][0];
                                ?>
                                <small><span class="form-text text-muted"><?php echo $errorName; ?></span></small>
                            <?php endif; ?>
                        </div>
                        <div class="form-group">
                            <label for="password">Password</label>
                            <input type="password" name="password" id="password" placeholder="" class="form-control" >
                            <?php
                            if (isset($_SESSION['errors']['password'])):
                                $errorPassword = $_SESSION['errors']['password'][0];
                                ?>
                                <small><span class="form-text text-muted"><?php echo $errorPassword; ?></span></small>
                            <?php endif; ?>
                        </div>

                        <button type="submmit" class="btn btn-secondary">Sign up</button>
                    </form>
                </div>
            </div>
        </div>

    </div>
</div>

{% endblock %}

















